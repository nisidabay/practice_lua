# Basics — Lua Fundamentals

Variables, types, arithmetic, and printing.

## Quick Start

```bash
lua hello.lua        # Variables, types, type()
lua basic.lua        # Variable declarations and expressions
lua arithmetic.lua   # All math operators
lua globals.lua      # Global vs local scope
lua printing.lua     # print() formatting
```

## Common Patterns

```lua
local name = "Carlos"
local count = 42
print("Hello, " .. name .. "!")
print(type(name))  --> string
```

## Now Build Your Own

Declare three variables (name, age, city), calculate age+5, print formatted: "Name lives in City, will be Age+5 in 5 years."
