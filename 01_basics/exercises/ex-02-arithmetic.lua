#!/usr/bin/env lua
-- Exercise: Arithmetic operators
--
-- Instructions: Complete the three print statements below by filling in the
-- missing arithmetic expressions for a+b, a*b, and a^b.

local a, b = 15, 7

-- TODO: Fill in the missing expressions below
local sum     = nil  -- should be a + b
local product  = nil  -- should be a * b
local power    = nil  -- should be a ^ b

-- Verification (DO NOT MODIFY)
print(string.format("%d + %d = %d", a, b, sum))
print(string.format("%d * %d = %d", a, b, product))
print(string.format("%d ^ %d = %d", a, b, power))
-- Solution: see exercises.lua
