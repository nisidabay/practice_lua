#!/usr/bin/env lua
-- Exercise: String concatenation and length
--
-- Instructions: Assign the correct values to `msg` and `len` below.
-- Use the `..` operator to concatenate and the `#` operator to get length.

-- TODO: Build a greeting "Hello, World!" and compute its length
local a, b = "Hello", "World"
local msg = nil
local len = nil

-- Verification (DO NOT MODIFY)
print("msg: " .. tostring(msg))
print("length: " .. tostring(len))
-- Solution: see exercises.lua
