#!/usr/bin/env lua
-- Exercise: string.sub
--
-- Instructions: Use string.sub (or :sub) to extract substrings from `text`.
-- Get the first 3 characters and the last 3 characters.

local text = "the quick brown fox"

-- TODO: Extract substrings using string.sub
local first_three = nil
local last_three = nil

-- Verification (DO NOT MODIFY)
print("first 3 chars: " .. tostring(first_three))
print("last 3 chars: " .. tostring(last_three))
-- Solution: see exercises.lua
