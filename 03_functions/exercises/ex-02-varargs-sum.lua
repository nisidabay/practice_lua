#!/usr/bin/env lua
-- Exercise: Varargs sum — Fill in the sum function body
--
-- The function sum(...) should accept a variable number of numeric arguments
-- and return their total. Use a loop over the {...} table.

-- TODO: implement sum body
local function sum(...)
    -- Sum all arguments and return the total
    return nil  -- placeholder
end

-- Verification (don't modify below):
print("sum(1,2,3,4,5) =", sum(1, 2, 3, 4, 5))
-- Expected: sum(1,2,3,4,5) = 15
-- Solution: see exercises.lua
